///getSpecialCharacter(str)
/*
    Returns the character cooresponding to a non-ASCII sprite string character
    
    argument0 - int - character index
    
    returns - string - character to put into the internal string so that
        draw_sprite_string functions draw the right character
*/
return chr(argument0);
